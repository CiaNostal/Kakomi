# Kakomi リファクタリング仕様書 for ver 2.0

## 1. プロジェクト概要
既存のウェブアプリ「Kakomi」を、**Fabric.js** を使用して全面的にリライトします。
現状の「静的なCanvas描画」から、「オブジェクト指向のインタラクティブな編集（ドラッグ移動、拡大縮小、回転）」ができるモダンなUIへ刷新することが目的です。

## 2. 技術スタック (CDNベース/ビルド不要)
以下のライブラリをCDN経由で読み込み、`index.html` 単体（または css/js 分離）で動作する構成にしてください。
ビルドツール（Webpack/Vite）は使用せず、ブラウザで直接開ける構成を維持します。

* **Core:** `Fabric.js (v5.x)` - キャンバス操作の中核。
* **UI Styling:** `Tailwind CSS (Play CDN)` - UIの装飾。
* **Logic:** `Vanilla JS` (または `Alpine.js` - 実装しやすい方を選択)
* **Utils:** `exif-js` - 写真のExif情報取得用。
* **Fonts:** `Google Fonts` (Noto Sans JP, Roboto, etc.)

## 3. 要件定義と実装仕様

### A. キャンバス設計 (Fabric.js)
* **Canvas構成:** 画面中央に配置。
* **レイアウトロジック:**
    * 「余白（フレーム）」の表現として、Canvasの背景色（単色 or ぼかし画像）を設定し、その中央に写真（Image Object）を配置する方式を採用すること。
    * 写真オブジェクトはロックせず、位置調整やリサイズを可能にするが、デフォルトでは中央揃えにする。
* **高解像度出力:**
    * 画面上のCanvasはプレビュー用のサイズ（例: 幅800px程度）に収める。
    * 保存時は `canvas.toDataURL({ multiplier: X })` を使用し、元の写真の解像度を維持した高画質で書き出すこと。

### B. オブジェクト操作 (UI/UX)
* **テキスト入力:**
    * 固定の入力欄を廃止し、**「レイヤー追加ボタン」**方式にする。
    * 追加されたテキストは、Canvas上でドラッグ移動、四隅ハンドルでのリサイズ、回転ができること。
* **プロパティ・インスペクタ (Contextual UI):**
    * **何も選択していない時:** キャンバス全体の設定（背景色、余白サイズ、画像ロードボタン）を表示。
    * **テキスト選択時:** そのテキスト専用の設定（フォント、サイズ、色、太字/斜体、整列、影）を表示するパネルに切り替える。
    * **双方向バインディング:** マウスでの変形操作と、サイドパネルの数値入力はリアルタイムに同期させること（Fabricの `object:modified`, `object:moving` イベントを使用）。

### C. 特殊機能：Exif連携
* **Exifプリセット機能:**
    * 「Exifを追加」ボタンを押した際、読み込まれている写真から `exif-js` でメタデータ（カメラ名、ISO、SS、F値、撮影日時）を取得する。
    * 取得したデータを整形し、**編集可能なテキストオブジェクト**としてCanvasに追加する（固定描画ではない）。
    * （例: テキストとして "Leica M11 | f/2.8 | 1/500" を生成し、あとはユーザーが自由にフォント変更や移動を行えるようにする）。

### D. デザイン要件 (Tailwind CSS)
* **全体レイアウト:**
    * **左サイドバー:** ツールボックス（レイヤー追加、保存ボタン）。
    * **中央:** キャンバスプレビューエリア（グレー背景などでキャンバスを目立たせる）。
    * **右サイドバー:** プロパティ設定パネル（選択中のオブジェクトに応じて内容が変化）。
* **モダンな操作感:**
    * ボタンには適切なホバーエフェクトをつける。
    * スライダーやカラーピッカーを活用し、直感的な操作にする。

## 4. 実装ステップの指示
以下の順序でコードを生成してください。ステップごとに動作確認を行います。

1.  **スケルトン作成:** HTML構造とライブラリの読み込み、レイアウトの枠組み（Tailwind）。
2.  **Fabric.js初期化:** キャンバスの設置と画像のドラッグ＆ドロップ読み込み機能。
3.  **Exif解析実装:** 画像読み込み時にExifを取得し、コンソールに出すテスト実装。
4.  **オブジェクト追加機能:** 「テキスト追加」「Exif情報追加（テキスト化）」の実装。
5.  **プロパティ連携:** 選択されたオブジェクトのプロパティを右サイドバーに表示・変更するロジック。
6.  **保存機能:** 高解像度での画像書き出し処理。